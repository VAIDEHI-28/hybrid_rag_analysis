from ingestion.registry import DataRegistry
from schema.schema_mapper import SchemaMapper

# TEMPORARY MANUAL TEST

# Assume registry already has data
registry = DataRegistry()

# Manually simulate what ingestion registered
# (In real flow, registry will be shared)
